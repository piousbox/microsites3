# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development: &id1
  secret_key_base: ca423e174eeeb76c659c13c4d254939e31fb395c305fe5b7800a8d224bab3b859a55cc6fdf5369d66fe5ae58e082cffd96479fd2246193c35f3ebb4a74cc9e5f
development_cached: *id1
development_production: *id1
development_docker: *id1

test:
  secret_key_base: 7dd1735dcc519af2f167d16ab3e58f597dcc0b83af2cd72a3eccc85f6a4cafd7c679fcd10572c093e95bda79875161d0dae0da955866b767d9bddeed9c60cc9a

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: this-secret-keybase
  trash: <%= ENV["SECRET_KEY_BASE"] %>
